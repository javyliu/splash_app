import 'package:flutter/material.dart';

const primaryColor = Color(0xffff7643);
const primaryLightColor = Color(0xffffecdf);
const primaryGradientColor = LinearGradient(begin: Alignment.topLeft, end: Alignment.bottomRight, colors: [
  Color(0xffffa53e), Color(0xffff7643),
]);
const secondaryColor = Color(0xff070707);
const textColor = Color(0xff757575);

const animationDuration = Duration(milliseconds: 200);
